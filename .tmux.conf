# kitty term have smt similar build-in

unbind r
bind r source-file ~/.tmux.config

set -g prefix C-s

#act like vim
setw -g mode-keys vi 
bind-key h select-pane —L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# List of plugins
set -g @plugin 'tmux-plugins/tpm'

# Initialize TMUX plugin manager(keep this line at the very bottom of tux. conf)
run '~/.tmux/plugins/tpm/tpm'
